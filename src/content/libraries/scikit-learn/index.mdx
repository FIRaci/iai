---
title: "scikit-learn"
category: "libraries"
icon: "brain"
difficulty: "beginner"
tags: ["cli", "autocomplete", "api"]
lastUpdated: "2026-05-26"
---

## Tổng quan

<Note type="info">
**scikit-learn** là machine learning library phổ biến nhất cho Python. Hỗ trợ classification, regression, clustering, và dimensionality reduction.
</Note>

## Cài đặt

<CodeBlock language="powershell" code={`pip install scikit-learn`} />

## Sử dụng

<CodeBlock language="python" code={`from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import train_test_split
from sklearn.metrics import accuracy_score

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2)
clf = RandomForestClassifier()
clf.fit(X_train, y_train)
predictions = clf.predict(X_test)
print(accuracy_score(y_test, predictions))`} />

## Liên kết

- [scikit-learn](https://scikit-learn.org)
- [Docs](https://scikit-learn.org/stable)
